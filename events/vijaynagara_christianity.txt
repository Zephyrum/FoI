namespace = foi

#Vijaynagara starts Hindu with Nasrani provinces.
#Christians will eventually want a Christian government

country_event = {
	id = foi.5
	title = "foi.eventname5"
	desc = "foi.eventdesc5"
	picture = RELIGIOUS_CONVERSION_eventPicture
	
	fire_only_once = yes
	
	trigger = {
		tag = VIJ
		is_subject = no
		NOT = { religion = nestorian }
		any_owned_province = {
			 religion = nestorian
		}
	}

	mean_time_to_happen = {
		months = 120
	}
	
	option = { #
		name = "foi.event5opt1"
		ai_chance = { factor = 0.5 }
		if = {
			limit = {
				NOT = { religion = nestorian }
			}
			change_religion = nestorian
			add_stability = -2
		}
	}
	
	option = { #
		name = "foi.event5opt2"
		ai_chance = { factor = 0.5 }
		add_prestige = 10
		random_owned_province = {
			limit = {
				religion_group = christian
				has_owner_religion = no
			}
			spawn_rebels = {
				type = religious_rebels
				size = 3
				win = yes
			}
		}
	}
}